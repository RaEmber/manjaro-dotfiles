#!/bin/bash

#if [ $(ls -l | wc -l) == 2 ]; then
echo "" > ~/.cache/wal/wal
#fi
wal -i ~/Bilder/wallpaper
file=$(cat ~/.cache/wal/wal)
mkdir -p ~/.color/wal/schemes
cd ~/.color
#if [ ! -L $file ]; then
ln -sf $file currentwallpaper
#fi
