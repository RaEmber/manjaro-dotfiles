#!/bin/bash

cd ~/Bilder/wallpaper
if [ $(ls -l | wc -l) == 2 ]; then
	echo "" > ~/.cache/wal/wal
fi
wal -i .
file=$(cat ~/.cache/wal/wal)
mkdir -p ~/.color/wal/schemes
cd ~/.color/wal/schemes
if [ ! -L $file ]; then
	ln -s ../../../Bilder/wallpaper/$file $file
fi
