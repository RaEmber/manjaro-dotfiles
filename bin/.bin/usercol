#!/bin/bash

if [ $# -eq 1 ]
then
	echo -e "Chose \e[1;34;40m$1\e[m as new color scheme"
	cd ~/.color
	wal -f user/$1.json
else
	echo -e "Applicable color schemes:\e[1;34;40m"
	ls ~/.color/user | sed "s/\.json//g"
fi
